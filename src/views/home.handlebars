<h2>Lista de Productos (Estática)</h2>

<ul>
    {{#each products}}
        <li>
            <strong>{{this.title}}</strong> - ${{this.price}} <br>
            Cod: {{this.code}}
        </li>
    {{/each}}
</ul>